#! /usr/local/bin/node --use_strict /usr/local/bin/jtree
#setup
#test stress test
 #setup
  require ../src/grammar/GrammarProgram.js
  %%|
   @grammar testlang
    @compiler txt
    @keywords
     baseNode
   @keyword baseNode
   @keyword block baseNode
    @keywords
     baseNode
   @keyword to block
    @columns word
    @compiler txt
     @sub to {word}
     @closeChildren end
 getRunTimeKeywordNames
  =# 3
 getDefinitionByName to
  =~ object
  getSuggestedSize
   =~ object
 getTopNodeTypes
  =# 3
 getTargetExtension
  == txt
 getExtensionName
  == testlang
 getDocs
  =~ string
 toSublimeSyntaxFile
  =~ string
 isDefined notDefined
  == false
 isDefined to
  == true